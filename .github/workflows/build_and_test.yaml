name: Build and test back-end repo

on: [pull_request]

jobs:
  test_backoffice:
    uses: ./.github/workflows/backoffice_test_workflow.yaml
  test_backend:
    uses: ./.github/workflows/backend_test_workflow.yml
